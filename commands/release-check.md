# Release Check Command

```text
Use repo-hygiene-team and tester-team.
Check release readiness: tests, docs, git status, forbidden tracked files, generated artifacts, secrets, public repo cleanliness, and known production risks.
Do not fix unless I explicitly switch workflow.
Safety: never use git add ., do not commit, tag, or push unless I explicitly allow it.
Expected output: pass/fail release checklist, findings, exact remediation steps.
```

