# Scoped Test Command

```text
Use tester-team for a scoped audit.
Scope: <feature/folder/phase/user flow>.
Read relevant context and logs, inspect only the affected area, run targeted tests if available, and report issues without fixing.
Safety: never use git add ., do not modify files, do not commit or push.
Expected output: scoped findings by severity, evidence, commands run, residual risks.
```

